const num1 = parseInt(prompt("We will now multiply two numbers! Enter a number" ));
const num2 = parseInt(prompt("Enter the second number you would like to multiply!"));
const multiply = num1 * num2;
alert(multiply);